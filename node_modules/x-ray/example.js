var Xray = require('./new')
var vo = require('vo')

var xray = Xray()

xray
  .get('http://google.com')
  .click('a.gb_P')
  .select({
    title: 'title @html',
    links: ['a@href']
  })
  .run(function(err, str) {
    if (err) throw err
    console.log('str', str);
  })
  .close()
